<Page xmlns="http://www.nativescript.org/tns.xsd"
      xmlns:g="components/grid-view/grid-view"
      navigatedTo="navigatedTo"
      >
  <StackLayout>
    <StackLayout bindingContext="{{ item }}">
      <Label text="{{ Title }}" />
    </StackLayout>
    <StackLayout bindingContext="{{ item }}">
      <Label text="{{ Title }}" />
    </StackLayout>
  </StackLayout>
</Page>