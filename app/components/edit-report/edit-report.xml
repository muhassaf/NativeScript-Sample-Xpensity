<Page xmlns="http://www.nativescript.org/tns.xsd"
      loaded="pageLoaded"
      navigatingTo="navigatedTo" >
  <Page.optionsMenu>
    <MenuItem text="{{ resources.done }}" tap="doneMenuItemTap" android.position="actionBar" />
  </Page.optionsMenu>
  <GridLayout>
    <ScrollView>
      <StackLayout>
        <GridLayout cssClass="firstTextFieldContainer">
          <TextField text="{{ item.Title }}" hint="Title" />
          <Border cssClass="pickerBorder" visibility="{{ iosVisibility }}" />
        </GridLayout>
        <GridLayout cssClass="textFieldContainer">
          <TextField text="{{ item.BusinessPurpose  }}" hint="Business Purpose" />
          <Border cssClass="pickerBorder" visibility="{{ iosVisibility }}" />
        </GridLayout>
        <GridLayout cssClass="textFieldContainer">
          <TextField text="{{ item.CostCenter }}" hint="Cost Center" />
          <Border cssClass="pickerBorder" visibility="{{ iosVisibility }}" />
        </GridLayout>
        
        <Button text="{{ resources.deleteReport }}" tap="deleteReportButtonTap" color="#FF0000" cssClass="secondaryButton" visibility="{{ editVisibility }}" />
      </StackLayout>
    </ScrollView>

    <ActivityIndicator busy="{{ isLoading }}" />
  </GridLayout>
</Page>