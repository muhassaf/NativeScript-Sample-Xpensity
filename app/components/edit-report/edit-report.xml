<Page xmlns="http://www.nativescript.org/tns.xsd"
      loaded="pageLoaded"
      navigatedTo="navigatedTo" >
  <Page.optionsMenu>
    <MenuItem text="DONE" tap="doneMenuItemTap" android.position="actionBar" />
  </Page.optionsMenu>
  <GridLayout>
    <ScrollView>
      <StackLayout>
        <GridLayout cssClass="TextFieldContainer">
          <TextField text="{{ item.Title }}" hint="Title" cssClass="firstTextField" />
        </GridLayout>
        <GridLayout cssClass="TextFieldContainer">
          <TextField text="{{ item.BusinessPurpose  }}" hint="Business Purpose" />
        </GridLayout>
        <GridLayout cssClass="TextFieldContainer">
          <TextField text="{{ item.CostCenter }}" hint="Cost Center" />
        </GridLayout>
        
        <Button text="DELETE REPORT" tap="deleteReportButtonTap" color="#FF0000" cssClass="secondaryButton" visibility="{{ editVisibility }}" />
      </StackLayout>
    </ScrollView>

    <ActivityIndicator busy="{{ isLoading }}" />
  </GridLayout>
</Page>